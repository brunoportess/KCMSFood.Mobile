<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="KCMSFood.Mobile.Views.CategoryFormPage"
             Title="Formulário de categoria">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="Nome" FontAttributes="Bold" />
            <Entry Text="{Binding Category.CategoryName}" />

            <Label Text="Descrição" FontAttributes="Bold" />
            <Entry Text="{Binding Category.CategoryDescription}" />

            <Button Style="{StaticResource buttonStyle}" VerticalOptions="EndAndExpand" Text="SALVAR" Command="{Binding SaveCommand}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>